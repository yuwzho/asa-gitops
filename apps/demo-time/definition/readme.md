# App demo-time definition

This folder contains terraform module to create app `demo-time`. It creates the `demo-time` with public endpoint exposed. Under the app, there are 2 deployments: `blue` and `green`.